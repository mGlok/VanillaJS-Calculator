'use strict';
const numbers = document.querySelectorAll('.numbers')
const operatos = document.querySelectorAll('.operator')
const clearEntry = document.querySelectorAll('.clear-enter')
const clear = document.querySelectorAll('clear')
const equal = document.querySelectorAll('.equal')
const previousOperand = document.querySelectorAll('.previous-operand')
const currentOperand = document.querySelectorAll('.current-operand')